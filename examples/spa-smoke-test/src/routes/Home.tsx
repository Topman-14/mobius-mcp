export function Home() {
  return (
    <div>
      <h2>Home</h2>
      <p>Navigate around using the links above — each is a client-side route change (React Router / history.pushState), not a full page load.</p>
    </div>
  );
}
