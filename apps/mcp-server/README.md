# @console-stream-mcp/mcp-server

MCP server that maintains a live, in-memory stream of browser runtime events and exposes them to AI agents as MCP tools.

## Status

Scaffold — implementation pending.
