{
  "name": "mobius-client",
  "version": "0.0.1",
  "description": "Drop-in client that streams browser runtime events to a local mobius-mcp server",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "devDependencies": {
    "@mobius-mcp/capture-core": "0.0.1",
    "tsup": "^8.3.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --target es2020 --clean"
  }
}
