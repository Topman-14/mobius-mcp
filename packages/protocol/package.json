{
  "name": "@console-stream-mcp/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "Event schema, message envelope, and versioning shared by all console-stream-mcp clients and the server",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
