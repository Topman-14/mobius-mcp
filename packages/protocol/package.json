{
  "name": "@mobius-mcp/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "Event schema, message envelope, and versioning shared by all mobius-mcp clients and the server",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch --preserveWatchOutput"
  }
}
